{
  "trigger": {
    "trigger_type": "trigger.manual",
    "params": {}
  },
  "condition": null,
  "actions": [
    {
      "id": "a1",
      "action_type": "camera.capture",
      "params": {
        "mode": "photo",
        "output_uri": "sandbox://captures/qr_{{metadata.run_id}}.jpg"
      }
    },
    {
      "id": "a2",
      "action_type": "transform.qr_decode",
      "params": {
        "media_uri": "{{state.capture_uri}}"
      }
    }
  ]
}
